services:
  - docker

install:
  - docker build .
  - echo $TRAVIS_BRANCH

script:
  - docker-compose up -d

deploy:
  provider: script
  script: bash scripts/deploy.sh production
  on:
    branch: master
deploy:
  provider: script
  script: bash scripts/deploy.sh latest
  on:
    branch: development
